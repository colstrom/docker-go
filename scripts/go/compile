#!/bin/sh

set -o errexit
set -o nounset
set -o xtrace

IN=${PWD}/${1}
OUT=${PWD}/${2}

export CGO_ENABLED=0
export GOROOT_BOOTSTRAP=${GOROOT_BOOTSTRAP:-'/usr/lib/go'}
export GOROOT_FINAL=${OUT}

cd ${IN}/src
./make.bash
